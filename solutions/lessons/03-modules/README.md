# Lesson 3 Solution

No answer key is included in this repository.

## How to validate your Lesson 3 implementation
- Confirm your module directory has `main.tf`, `variables.tf`, and `outputs.tf`.
- Confirm at least one variable has validation.
- Confirm the root configuration calls the module.
- Confirm module outputs are re-exposed by the root.
- Compare with `lessons/03-modules/checks.md`.
