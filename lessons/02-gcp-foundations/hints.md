# Hints

- Keep org and folder concepts as comments if you cannot create them safely.
- Prefer one API enablement resource per service.
- Use separate variables for project ID and service account name.
- Do not overbuild the network in this lesson.
